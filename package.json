{
  "name": "wstg-bilingual-deepseek",
  "private": true,
  "type": "module",
  "description": "Dev-only tooling for the WSTG Bilingual reader. The app itself is zero-build — open index.html.",
  "scripts": {
    "validate": "node tools/validate-data.mjs",
    "build:site": "node tools/build-site.mjs",
    "serve": "node tests/static-server.mjs",
    "serve:site": "node tests/static-server.mjs 8900 --dir _site",
    "test:e2e": "playwright test",
    "test:unit": "node --test tests/unit/*.test.mjs",
    "test": "npm run validate && npm run test:unit && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
